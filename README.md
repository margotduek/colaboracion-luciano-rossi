# Kit comercial · Canal Argentina y Cono Sur

Página publicada con GitHub Pages.

Incluye el kit de ventas y el esquema de comisiones en PDF (embebidos en `index.html`).
El convenio comercial no se publica aquí: se envía por canal privado.

La página lleva `noindex` y `robots.txt` con `Disallow: /`.
